---
label: Text block
hide_body: true
fields:
- name: slug
  type: text
  config:
    required: false
  label: Slug
- name: image
  type: file
  config:
    maxSize: 250
  label: Image
- name: title
  type: text
  config:
    required: false
  label: Title
- name: description
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: true
    schema:
      format: markdown
  label: Description
- name: link
  type: blocks
  label: Link
  template_types:
  - link
  config:
    min: 
    max: 
